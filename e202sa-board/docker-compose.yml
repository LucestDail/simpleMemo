# e202sa-board - 로컬/내부망 실행
# 사용: docker compose up -d
# 접속: http://localhost:3000 (컨트롤러), http://localhost:3000/board (보드)

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
    restart: unless-stopped
